# ===========================
# automations.yaml
# ===========================

# ───────────────────────────────────────────────
#  BMS BLE – reload after boot with retry
# ───────────────────────────────────────────────
# The bms_ble integration checks for the BLE device at setup.
# If the ESP32 BLE proxy hasn't reconnected yet, setup fails
# or gets cancelled. This automation retries the reload
# every 30s (up to 30 attempts = ~15 min window) until BMS comes online.
# HA's own setup retry (every 80 s) covers most of this; the loops mainly
# add log noise. What kept the BMS down for hours (09-06: 3 h, 09-16: 7 h)
# was the HA box's Realtek USB Bluetooth dongle: it connects to the JBD
# but never gets a reply, and HA preferred it for its stronger signal.
# Its bluetooth config entry is disabled since 2026-09-16, so the BMS
# goes through the a32-pro proxy (.github/copilot-instructions.md,
# "Bluetooth").

- id: bms_ble_reload_after_boot
  alias: "BMS BLE - reload integration after boot"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start
  action:
    - delay:
        seconds: 20
    - repeat:
        while:
          - condition: state
            entity_id: sensor.olins_van_bms_battery
            state: "unavailable"
          - "{{ repeat.index <= 30 }}"
        sequence:
          - action: homeassistant.reload_config_entry
            data:
              entry_id: "{{ config_entry_id('sensor.olins_van_bms_battery') }}"
          - delay:
              seconds: 30

# ───────────────────────────────────────────────
#  BMS BLE – runtime recovery when unavailable
# ───────────────────────────────────────────────
# If BMS goes unavailable outside of boot (BLE proxy dropout,
# connection slot exhaustion, etc.), wait 60s then reload.
# Max 5 retries, resets after BMS comes back.

- id: bms_ble_runtime_recovery
  alias: "BMS BLE - reload when unavailable (runtime)"
  mode: single
  trigger:
    - trigger: state
      entity_id: sensor.olins_van_bms_battery
      to: "unavailable"
      for:
        seconds: 60
  action:
    - repeat:
        while:
          - condition: state
            entity_id: sensor.olins_van_bms_battery
            state: "unavailable"
          - "{{ repeat.index <= 5 }}"
        sequence:
          - action: homeassistant.reload_config_entry
            data:
              entry_id: "{{ config_entry_id('sensor.olins_van_bms_battery') }}"
          - delay:
              seconds: 60

# ───────────────────────────────────────────────
#  Filtered GPS tracker – MQTT update logic
# ───────────────────────────────────────────────

# 1️⃣  (Optional) Republish last known fix on HA boot
#     – only if broker has NO retained packet
- id: starlink_bootstrap_if_missing
  alias: "GPS • bootstrap retained message if none"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start
  condition:
    # Run only when tracker state is 'unknown' (means broker lost retain)
    - "{{ states('device_tracker.ublox_gps_filtered') in ['unknown', 'unavailable'] }}"
    # …and only if the raw tracker actually has a real fix (never bootstrap 0,0).
    - condition: template
      value_template: >
        {{ state_attr('device_tracker.ublox_gps','latitude') | float(0) != 0
           and state_attr('device_tracker.ublox_gps','longitude') | float(0) != 0 }}
  action:
    - action: mqtt.publish
      data:
        topic: homeassistant/device_tracker/ublox_gps_filtered/attributes
        qos: 1
        retain: true
        payload: >
          {
            "latitude": {{ state_attr('device_tracker.ublox_gps','latitude') | float(0) }},
            "longitude": {{ state_attr('device_tracker.ublox_gps','longitude') | float(0) }},
            "gps_accuracy": {{ state_attr('device_tracker.ublox_gps','gps_accuracy') | float(0) }},
            "elevation": {{ state_attr('device_tracker.ublox_gps','altitude') | float(0) }},
            "source_type": "gps"
          }
    - action: mqtt.publish
      data:
        topic: homeassistant/device_tracker/ublox_gps_filtered/state
        qos: 1
        retain: true
        payload: "home"

# 2️⃣  While the van is moving: publish new GPS fix each second
- id: starlink_filtered_while_moving
  alias: "GPS • filtered coords 1 Hz while moving"
  mode: queued
  max: 3
  trigger:
    - trigger: time_pattern
      seconds: "/1"
  condition:
    - condition: state
      entity_id: binary_sensor.vehicle_is_moving
      state: "on"
    # Don't republish a (0,0) fix if the raw tracker momentarily has no fix.
    - condition: template
      value_template: >
        {{ state_attr('device_tracker.ublox_gps','latitude') | float(0) != 0
           and state_attr('device_tracker.ublox_gps','longitude') | float(0) != 0 }}
  action:
    - action: mqtt.publish
      data:
        topic: homeassistant/device_tracker/ublox_gps_filtered/attributes
        qos: 1
        retain: true
        payload: >
          {
            "latitude": {{ state_attr('device_tracker.ublox_gps','latitude') | float(0) }},
            "longitude": {{ state_attr('device_tracker.ublox_gps','longitude') | float(0) }},
            "gps_accuracy": {{ state_attr('device_tracker.ublox_gps','gps_accuracy') | float(0) }},
            "elevation": {{ state_attr('device_tracker.ublox_gps','altitude') | float(0) }},
            "source_type": "gps"
          }
    # Tracker’s textual state stays constant
    - action: mqtt.publish
      data:
        topic: homeassistant/device_tracker/ublox_gps_filtered/state
        qos: 1
        retain: true
        payload: "home"

# 4️⃣  Snapshot when movement STARTS
- id: starlink_filtered_start_move
  alias: "GPS • snapshot when movement starts"
  trigger:
    - trigger: state
      entity_id: binary_sensor.vehicle_is_moving
      from: "off"
      to: "on"
  condition:
    # Skip the snapshot if the raw tracker has no real fix (avoids a 0,0 jump).
    - condition: template
      value_template: >
        {{ state_attr('device_tracker.ublox_gps','latitude') | float(0) != 0
           and state_attr('device_tracker.ublox_gps','longitude') | float(0) != 0 }}
  action:
    - action: mqtt.publish
      data:
        topic: homeassistant/device_tracker/ublox_gps_filtered/attributes
        qos: 1
        retain: true
        payload: >
          {
            "latitude": {{ state_attr('device_tracker.ublox_gps','latitude') | float(0) }},
            "longitude": {{ state_attr('device_tracker.ublox_gps','longitude') | float(0) }},
            "gps_accuracy": {{ state_attr('device_tracker.ublox_gps','gps_accuracy') | float(0) }},
            "elevation": {{ state_attr('device_tracker.ublox_gps','altitude') | float(0) }},
            "source_type": "gps"
          }
    - action: mqtt.publish
      data:
        topic: homeassistant/device_tracker/ublox_gps_filtered/state
        qos: 1
        retain: true
        payload: "home"

# 5️⃣  Snapshot when movement STOPS
- id: starlink_filtered_stop_move
  alias: "GPS • snapshot when movement stops"
  trigger:
    - trigger: state
      entity_id: binary_sensor.vehicle_is_moving
      from: "on"
      to: "off"
  condition:
    # Skip the snapshot if the raw tracker has no real fix (avoids a 0,0 jump).
    - condition: template
      value_template: >
        {{ state_attr('device_tracker.ublox_gps','latitude') | float(0) != 0
           and state_attr('device_tracker.ublox_gps','longitude') | float(0) != 0 }}
  action:
    - action: mqtt.publish
      data:
        topic: homeassistant/device_tracker/ublox_gps_filtered/attributes
        qos: 1
        retain: true
        payload: >
          {
            "latitude": {{ state_attr('device_tracker.ublox_gps','latitude') | float(0) }},
            "longitude": {{ state_attr('device_tracker.ublox_gps','longitude') | float(0) }},
            "gps_accuracy": {{ state_attr('device_tracker.ublox_gps','gps_accuracy') | float(0) }},
            "elevation": {{ state_attr('device_tracker.ublox_gps','altitude') | float(0) }},
            "source_type": "gps"
          }
    - action: mqtt.publish
      data:
        topic: homeassistant/device_tracker/ublox_gps_filtered/state
        qos: 1
        retain: true
        payload: "home"

- alias: "Main Lighting: Sync LED Dimmer with Switch"
  trigger:
    - trigger: state
      entity_id: light.main_lighting_switch
  action:
    - choose:
        - conditions: "{{ trigger.to_state.state == 'on' }}"
          sequence:
            - action: light.turn_on
              data:
                brightness:
                  "{{ state_attr('light.main_lighting_switch', 'brightness')
                  | default(255) }}"
              target:
                entity_id: light.led_controller_cct_1
        - conditions: "{{ trigger.to_state.state == 'off' }}"
          sequence:
            - action: light.turn_off
              target:
                entity_id: light.led_controller_cct_1
  mode: single
  id: da628a416f844416bc745bc6c33ec37c

- alias: "Main Lighting: Sync Warmth to LED Dimmer"
  trigger:
    - trigger: state
      entity_id: input_number.main_light_warmth
  action:
    - action: light.turn_on
      data:
        # input_number is in mireds (250-500); HA now requires kelvin. Convert.
        color_temp_kelvin: "{{ (1000000 / (states('input_number.main_light_warmth') | int)) | int }}"
      target:
        entity_id: light.led_controller_cct_1
  mode: single
  id: 7d383f4cc6ea4901b42f9a92288acfa1

- id: "1739334868426"
  alias: Linked Entities
  description: ""
  use_blueprint:
    path: alexdelprete/ha-blueprint-linked-entities.yaml
    input:
      linked_entity:
        - light.led_controller_cct_1
        - light.main_lighting_switch

- id: "1739334926562"
  alias: Linked Entities Under Cabinet
  description: ""
  use_blueprint:
    path: alexdelprete/ha-blueprint-linked-entities.yaml
    input:
      linked_entity:
        - light.led_controller_cct_2
        - light.under_cabinet_lighting_switch

- id: "1739334948121"
  alias: Linked Entities Shower Lighting
  description: ""
  use_blueprint:
    path: alexdelprete/ha-blueprint-linked-entities.yaml
    input:
      linked_entity:
        - light.shower_lighting_switch
        - light.led_controller_sc_1

# ────────────────────────────────────────────────────────────────
# Sundown dimming – indoor lights get a bit dimmer at sundown and a bit more
# once it's dark. Phase comes from sensor.indoor_light_phase (day / twilight /
# dark); the ceilings from input_number.indoor_light_sundown_pct / _dark_pct;
# master enable input_boolean.indoor_light_sun_dimming.
#   • Lights already on step down to the new ceiling over ~2 min.
#   • Lights switched on during twilight/dark come on no brighter than it.
#   • The first switch-on after sunrise of a light left at one of those
#     ceilings goes back to full, so it isn't stuck at last night's level.
# Hands off: Shower light while Shower Mode is on, Cabinet light while the LPG
# valve is open (Cook Mode sets it to 100 %), and everything during Sleep
# Mode's wind-down and the 2 min after Sleep Mode turns off (wake-up restore).
# Only the LED controllers are commanded; the Inovelli switches follow via the
# Linked Entities blueprint (commanding both cross-fires, see
# sleep_mode_on_actions).
# ────────────────────────────────────────────────────────────────
- id: indoor_lights_sundown_dim
  alias: "Indoor Lights – Sundown dim for lights already on"
  # restart: a newer phase change supersedes an unfinished fade
  mode: restart
  trigger:
    # Evening edges only. At dawn the phase goes dark → twilight, and lights
    # must not dim then.
    - trigger: state
      entity_id: sensor.indoor_light_phase
      from: "day"
      to: "twilight"
    - trigger: state
      entity_id: sensor.indoor_light_phase
      from: "twilight"
      to: "dark"
  condition:
    - condition: state
      entity_id: input_boolean.indoor_light_sun_dimming
      state: "on"
    - "{{ not (is_state('input_boolean.sleep_mode', 'on') and is_state('input_boolean.sleep_winddown_complete', 'off')) }}"
  action:
    - variables:
        steps: 12
        cap: >-
          {{ ((state_attr('sensor.indoor_light_phase', 'max_brightness_pct') | float(100)) * 2.55) | round(0) | int }}
        # [entity, starting brightness] for each light that is on, above the
        # ceiling, and not being held bright by a mode.
        start: >-
          {% set ns = namespace(out=[]) %}
          {% for id in ['light.led_controller_cct_1', 'light.led_controller_cct_2',
                        'light.led_controller_sc_1', 'light.led_controller_sc_2'] %}
            {% set b = state_attr(id, 'brightness') | int(0) %}
            {% set held = (id == 'light.led_controller_sc_1' and is_state('input_boolean.shower_mode', 'on'))
                       or (id == 'light.led_controller_cct_2' and is_state('switch.a32_pro_switch16_lpg_valve', 'on')) %}
            {% if is_state(id, 'on') and b > cap + 2 and not held %}
              {% set ns.out = ns.out + [[id, b]] %}
            {% endif %}
          {% endfor %}
          {{ ns.out }}
    # Small steps rather than one long transition: the main light's switch sync
    # re-sends the level with no transition, which would cut a long fade short,
    # and a ≤5 % jump every 10 s is invisible either way.
    - if: "{{ start | count > 0 }}"
      then:
        - repeat:
            count: "{{ steps }}"
            sequence:
              # This step's target vs the previous step's (what was last set).
              # A light brighter than that was turned up by hand — leave it.
              - variables:
                  targets: >-
                    {% set ns = namespace(out=[]) %}
                    {% for id, b0 in start %}
                      {% set prev = (b0 - (b0 - cap) * (repeat.index - 1) / steps) | round(0) | int %}
                      {% set goal = (b0 - (b0 - cap) * repeat.index / steps) | round(0) | int %}
                      {% set b = state_attr(id, 'brightness') | int(0) %}
                      {% if is_state(id, 'on') and b > goal + 2 and b <= prev + 8 %}
                        {% set ns.out = ns.out + [[id, goal]] %}
                      {% endif %}
                    {% endfor %}
                    {{ ns.out }}
              - repeat:
                  for_each: "{{ targets }}"
                  sequence:
                    - action: light.turn_on
                      target:
                        entity_id: "{{ repeat.item[0] }}"
                      data:
                        brightness: "{{ repeat.item[1] }}"
                        transition: 10
              - delay: 10

- id: indoor_lights_sundown_turn_on_level
  alias: "Indoor Lights – Sundown turn-on level / morning reset"
  # parallel: lights often come on together (voice, the Lights tile, Power
  # Saving restore) and each needs its own check
  mode: parallel
  max: 10
  trigger:
    - trigger: state
      entity_id:
        - light.led_controller_cct_1
        - light.led_controller_cct_2
        - light.led_controller_sc_1
        - light.led_controller_sc_2
      from: "off"
      to: "on"
  condition:
    - condition: state
      entity_id: input_boolean.indoor_light_sun_dimming
      state: "on"
    - "{{ not (is_state('input_boolean.sleep_mode', 'on') and is_state('input_boolean.sleep_winddown_complete', 'off')) }}"
    - "{{ now() - states.input_boolean.sleep_mode.last_changed > timedelta(minutes=2) }}"
    - "{{ not (trigger.entity_id == 'light.led_controller_sc_1' and is_state('input_boolean.shower_mode', 'on')) }}"
    - "{{ not (trigger.entity_id == 'light.led_controller_cct_2' and is_state('switch.a32_pro_switch16_lpg_valve', 'on')) }}"
  action:
    - variables:
        phase: "{{ states('sensor.indoor_light_phase') }}"
        level: "{{ trigger.to_state.attributes.brightness | int(0) }}"
        cap: >-
          {{ ((state_attr('sensor.indoor_light_phase', 'max_brightness_pct') | float(100)) * 2.55) | round(0) | int }}
        sundown_cap: >-
          {{ ((states('input_number.indoor_light_sundown_pct') | float(0)) * 2.55) | round(0) | int }}
        dark_cap: >-
          {{ ((states('input_number.indoor_light_dark_pct') | float(0)) * 2.55) | round(0) | int }}
        # Roughly today's sunrise: once the sun is up, next_rising is tomorrow's.
        sunrise: >-
          {% set r = state_attr('sun.sun', 'next_rising') %}
          {{ (as_datetime(r) - timedelta(days=1)).isoformat() if r else '' }}
    - choose:
        - conditions: "{{ phase in ['twilight', 'dark'] and level > cap + 2 }}"
          sequence:
            - action: light.turn_on
              target:
                entity_id: "{{ trigger.entity_id }}"
              data:
                brightness: "{{ cap }}"
                transition: 1
        # Off since before sunrise and still sitting at a ceiling this set last
        # night → back to full. A level you picked yourself is left alone.
        - conditions: >-
            {{ phase == 'day' and sunrise != ''
               and trigger.from_state.last_changed < as_datetime(sunrise)
               and ((level - sundown_cap) | abs <= 3 or (level - dark_cap) | abs <= 3) }}
          sequence:
            - action: light.turn_on
              target:
                entity_id: "{{ trigger.entity_id }}"
              data:
                brightness: 255
                transition: 1

- id: shore_charger_manual_enable_sync
  alias: Shore Charger – Manual Enable Sync
  mode: single
  trigger:
    - trigger: state
      entity_id: input_boolean.shore_power_charger_enabled
  action:
    - choose:
        - conditions:
            - condition: state
              entity_id: input_boolean.shore_power_charger_enabled
              state: "on"
          sequence:
            - action: switch.turn_on
              target:
                entity_id: switch.a32_pro_do8_switch04_shore_power_charger
        - conditions:
            - condition: state
              entity_id: input_boolean.shore_power_charger_enabled
              state: "off"
          sequence:
            - action: switch.turn_off
              target:
                entity_id: switch.a32_pro_do8_switch04_shore_power_charger

- id: shore_charger_reset_on_low_soc
  alias: Shore Charger – Reset when SOC below threshold
  description: "Power-cycle charger (OFF 10s → ON) when SOC drops below threshold"
  mode: single
  trigger:
    - trigger: numeric_state
      entity_id: sensor.olins_van_bms_battery # 0–100 %
      below: input_number.shore_charge_reset_threshold
      # Optional (recommended): require it to stay below for a few seconds to avoid noise
      for: "00:00:05"
  condition:
    - condition: state
      entity_id: input_boolean.shore_power_charger_enabled
      state: "on" # only if feature is enabled
    - condition: state
      entity_id: switch.a32_pro_do8_switch04_shore_power_charger
      state: "on" # only makes sense to reset if it's currently on
  action:
    - action: switch.turn_off
      target:
        entity_id: switch.a32_pro_do8_switch04_shore_power_charger
    - delay: "00:00:10" # reset window
    - action: switch.turn_on
      target:
        entity_id: switch.a32_pro_do8_switch04_shore_power_charger

# ────────────────────────────────────────────────────────────────
# Ducking in pulseaudio
# ────────────────────────────────────────────────────────────────
# The following automation is used to setup the ducking module in pulse audio which lowers the volume of music from airplay/audio from PC when HA is playing audio through the VLC media player
- alias: "Bootstrap ducking → VLC"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start # fires when HA has finished booting
  action:
    - delay: "00:00:15" # let hassio_audio come online
    - action: shell_command.load_ducking_docker

# ────────────────────────────────────────────────
# iPhone → Occupancy sensor replacement
# ────────────────────────────────────────────────

# 1. Welcome / lights when presence detected
- id: iphone_home_arrival # keep id for history
  alias: "iPhone Home Arrival - Lights & Welcome"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.van_occupied # debounced radar (smooths flicker)
      from: "off" # occupancy confirmed
      to: "on"
  condition:
    # Suppress this welcome flow while sleeping
    - condition: state
      entity_id: input_boolean.sleep_mode
      state: "off"
  action:
    - action: script.power_saving_mode_off
    - if:
        - condition: template
          # away ≥ 15 min
          value_template: >
            {% set away = now() - trigger.from_state.last_changed %}
            {{ away.total_seconds() >= 900 }}
      then:
        - action: media_player.volume_set
          target:
            entity_id: media_player.vlc_telnet
          data:
            volume_level: 0.20

        - delay: "00:00:02"

        # Calculate how long you were away and get battery percentage
        - action: tts.speak
          target:
            entity_id: tts.openai_tts_gpt_4o_mini_tts
          data:
            cache: true
            media_player_entity_id: media_player.vlc_telnet
            message: >
              {% set away_duration = now() - trigger.from_state.last_changed %}
              {% set total_minutes = (away_duration.total_seconds() / 60) | round(0) %}
              {% set days = (total_minutes / 1440) | int %}
              {% set hours = ((total_minutes % 1440) / 60) | int %}
              {% set minutes = (total_minutes % 60) | int %}
              {% set van_battery_pct = states('sensor.olins_van_bms_battery') | default('unknown') %}
              {% set inside_temp = states('sensor.a32_pro_bme280_1_temperature') | default('unknown') %}
              {% set outside_temp = states('sensor.a32_pro_bme280_4_temperature') | default('unknown') %}
              Welcome back. You were away for 
              {%- if days > 0 -%}
                {{ days }} day{% if days != 1 %}s{% endif %}
                {%- if hours > 0 %} and {{ hours }} hour{% if hours != 1 %}s{% endif %}{% endif -%}
              {%- elif hours > 0 -%}
                {{ hours }} hour{% if hours != 1 %}s{% endif %}
                {%- if minutes > 0 %} and {{ minutes }} minute{% if minutes != 1 %}s{% endif %}{% endif -%}
              {%- else -%}
                {{ minutes }} minute{% if minutes != 1 %}s{% endif %}
              {%- endif %}. 
              {% if van_battery_pct != 'unknown' %}
              The van battery is at {{ van_battery_pct }} percent.
              {% else %}
              I couldn't get the van battery level.
              {% endif %}
              {% if inside_temp != 'unknown' and outside_temp != 'unknown' %}
              It's {{ inside_temp | round(1) }} degrees inside and {{ outside_temp | round(1) }} degrees outside.
              {% elif inside_temp != 'unknown' %}
              The inside temperature is {{ inside_temp | round(1) }} degrees.
              {% elif outside_temp != 'unknown' %}
              The outside temperature is {{ outside_temp | round(1) }} degrees.
              {% endif %}

# 2. Auto-power-saving when leaving / returning
- id: auto_power_saving_when_away
  alias: "Auto Power Saving When Away from Van"
  mode: single
  max_exceeded: silent # both monitor rockers pressed back to back
  trigger:
    - id: leave_van
      trigger: state
      entity_id: binary_sensor.van_occupied # debounced radar (smooths flicker)
      from: "on"
      to: "off"
      for: "00:01:00" # away for ≥ 1 min
    - id: return_van
      trigger: state
      entity_id: binary_sensor.van_occupied # debounced radar (smooths flicker)
      from: "off"
      to: "on"
    # A monitor rocker pressed by hand means someone is here, so don't wait
    # for the radar (its 2 s debounce, or it missing someone sitting still).
    # It also covers the radar losing power: from 2026-09-13 to 09-16 it was
    # fed from the top monitor's relay, which power saving switches off.
    - id: return_rocker
      trigger: state
      entity_id:
        - switch.a32_pro_do8_switch06_top_monitor
        - switch.a32_pro_do8_switch07_bottom_monitor
      from: "off"
      to: "on"
  condition:
    # Let sleep mode fully control the environment without presence flips
    - condition: state
      entity_id: input_boolean.sleep_mode
      state: "off"
  action:
    - choose:
        # Enable power-saving after 1 min away
        - conditions: "{{ trigger.id == 'leave_van' and is_state('input_boolean.power_saving_mode','off') }}"
          sequence:
            - action: script.power_saving_mode_on
            - action: system_log.write
              data:
                level: info
                logger: automation.power_saving
                message: "Power saving mode enabled – away for ≥1 min"
        # Disable when back (if not driving). Only a physical rocker press
        # counts: it has no user and no parent context. A dashboard toggle
        # (maybe from away) or our own scene restore does not.
        - conditions: >
            {{ (trigger.id == 'return_van'
                or (trigger.id == 'return_rocker'
                    and trigger.to_state.context.user_id is none
                    and trigger.to_state.context.parent_id is none))
               and is_state('input_boolean.power_saving_mode','on')
               and is_state('binary_sensor.vehicle_is_moving','off') }}
          sequence:
            - action: script.power_saving_mode_off
            - action: system_log.write
              data:
                level: info
                logger: automation.power_saving
                message: >-
                  Power saving mode disabled –
                  {{ 'monitor rocker pressed' if trigger.id == 'return_rocker' else 'presence detected' }}

# ────────────────────────────────────────────────────────────────
# Power Saving Mode Automation - triggers scripts when toggle changes
# ────────────────────────────────────────────────────────────────
- id: power_saving_mode_automation
  alias: "Power Saving Mode Toggle Handler"
  description: "Triggers power saving scripts when toggle changes"
  mode: single
  trigger:
    - trigger: state
      entity_id: input_boolean.power_saving_mode
  action:
    - choose:
        - conditions:
            - condition: state
              entity_id: input_boolean.power_saving_mode
              state: "on"
          sequence:
            - action: script.power_saving_mode_on
        - conditions:
            - condition: state
              entity_id: input_boolean.power_saving_mode
              state: "off"
          sequence:
            - action: script.power_saving_mode_off

# ────────────────────────────────────────────────────────────────
# Fill-up Detection — Auto-update VE Correction
# ────────────────────────────────────────────────────────────────
# Fires when the smoothed tank level (sensor.wican_fuel_5_min_mean) jumps up by
# more than 15 percentage points (can't happen from normal consumption — must be
# a fill-up). After a long engine-off + fill, the 5-min mean's first reading on
# restart steps straight to the new high level, so a single >15 jump is reliable.
# Calls ve_update.py which:
#   1. Queries /vanlife/fuel-stats for the period since the last fill-up
#   2. Computes the actual fuel used (OBD tank delta) over that window
#   3. Derives a new VE correction factor (damped) and appends it to history
#   4. Updates input_number.fuel_ve_correction
- id: fill_up_detected
  alias: "Fill-up Detected — Auto-Update VE Correction"
  description: >
    Detects a fuel fill-up (5-min mean tank level jumps > 15% to a high level)
    and automatically recalibrates the speed-density VE correction factor.
  mode: single
  max_exceeded: silent
  trigger:
    - trigger: state
      entity_id: sensor.wican_fuel_5_min_mean
  condition:
    - condition: template
      value_template: >
        {% set new_fuel = trigger.to_state.state  | float(-1) %}
        {% set old_fuel = trigger.from_state.state | float(-1) %}
        {# A real fill jumps the SMOOTHED mean by >15 pts to a plausibly-high
           level. WiCAN-reconnect and slosh artifacts don't clear all three
           gates (old>0 rules out cold-boot 'unknown'; new>25 rules out noise). #}
        {{ old_fuel > 0 and new_fuel > 25 and (new_fuel - old_fuel) > 15 }}
  action:
    - variables:
        # from_ts = when was the previous fill-up (epoch ms stored in input_text)
        from_ts: "{{ states('input_text.last_fillup_ts') | int(0) }}"
        # to_ts = timestamp of the last stable reading before the jump happened
        to_ts: "{{ (trigger.from_state.last_updated.timestamp() * 1000) | int }}"
        fuel_before: "{{ trigger.from_state.state | float(0) | round(1) }}"
        fuel_after: "{{ trigger.to_state.state | float(0) | round(1) }}"
    # Run the VE update script (queries GPS API, computes correction, stores history)
    - action: shell_command.update_ve_from_fillup
      data:
        from_ts: "{{ from_ts }}"
        to_ts: "{{ to_ts }}"
    # Store the fill-up timestamp for next time (epoch ms at the "tank is now full" point)
    - action: input_text.set_value
      target:
        entity_id: input_text.last_fillup_ts
      data:
        value: "{{ (now().timestamp() * 1000) | int }}"
    # Notify so there's a record
    - action: persistent_notification.create
      data:
        title: "⛽ Fill-up Detected"
        message: >
          Tank: {{ fuel_before }}% → {{ fuel_after }}%
          (+{{ (fuel_after - fuel_before) | round(1) }}%,
          ~{{ ((fuel_after - fuel_before) / 100 * 94.6) | round(1) }} L)

          VE correction updated from GPS distance + tank delta.
          Check input_number.fuel_ve_correction for the new value.
        notification_id: fillup_detected

# ────────────────────────────────────────────────────────────────
# Live Trip computer — baseline reset / end stamping
# ────────────────────────────────────────────────────────────────
# Captures the trip baselines (odometer, fuel total, start time) when a NEW drive
# begins. A trip is anchored to actual MOVEMENT (not engine-on), so merely
# starting the engine to idle/warm up does NOT begin a trip — only driving does.
# "New" = the van had been NOT MOVING (idle OR engine-off, uniformly) for longer
# than input_number.trip_split_minutes, or no trip is recorded yet. Shorter
# stops (the usual 5-30 min fuel/coffee/errand stops) keep accumulating the same
# trip — including any idle fuel burned during them. trigger.from_state is the
# vehicle_is_moving 'off' state, whose last_changed marks when motion stopped, so
# the gap spans the whole stop even across an engine-off period.
- id: live_trip_reset_on_drive_start
  alias: "Live Trip • reset on new drive"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.vehicle_is_moving
      from: "off"
      to: "on"
  condition:
    - condition: template
      value_template: >
        {% set gap = (now() - trigger.from_state.last_changed).total_seconds()
                     if trigger.from_state else 999999 %}
        {% set split = states('input_number.trip_split_minutes') | float(35) * 60 %}
        {{ states('input_text.trip_start_ts') | int(0) == 0 or gap > split }}
  action:
    - action: input_number.set_value
      target: { entity_id: input_number.trip_baseline_distance_km }
      data: { value: "{{ states('sensor.vehicle_distance_odometer') | float(0) }}" }
    - action: input_number.set_value
      target: { entity_id: input_number.trip_baseline_fuel_l }
      data: { value: "{{ states('sensor.estimated_fuel_used_total_l') | float(0) }}" }
    # Movement-gated baselines (fuel + time burned/elapsed only while moving).
    - action: input_number.set_value
      target: { entity_id: input_number.trip_baseline_moving_fuel_l }
      data: { value: "{{ states('sensor.moving_fuel_total') | float(0) }}" }
    - action: input_number.set_value
      target: { entity_id: input_number.trip_baseline_moving_s }
      data: { value: "{{ states('sensor.moving_seconds_total') | float(0) }}" }
    # City/highway split baselines (only the highway side; city = total − highway).
    - action: input_number.set_value
      target: { entity_id: input_number.trip_baseline_highway_distance_km }
      data: { value: "{{ states('sensor.highway_distance_total') | float(0) }}" }
    - action: input_number.set_value
      target: { entity_id: input_number.trip_baseline_highway_fuel_l }
      data: { value: "{{ states('sensor.highway_fuel_total') | float(0) }}" }
    - action: input_text.set_value
      target: { entity_id: input_text.trip_start_ts }
      data: { value: "{{ (now().timestamp() * 1000) | int }}" }
    # Clear the previous end so the duration counts live again from now.
    - action: input_text.set_value
      target: { entity_id: input_text.trip_end_ts }
      data: { value: "0" }

# Stamps the trip end when the engine stops, so the card freezes the trip
# duration at the stop instead of ticking forever while parked. (A brief stop
# under 20 min won't reset the trip — see the automation above — so the duration
# simply resumes counting when the engine restarts.)
- id: live_trip_mark_end_on_stop
  alias: "Live Trip • mark end on engine off"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.engine_is_running
      from: "on"
      to: "off"
  action:
    - action: input_text.set_value
      target: { entity_id: input_text.trip_end_ts }
      data: { value: "{{ (now().timestamp() * 1000) | int }}" }

# ────────────────────────────────────────────────────────────────
# Auto Power Saving When Driving
# ────────────────────────────────────────────────────────────────
- id: auto_power_saving_when_driving
  alias: "Auto Power Saving When Driving"
  description: "Turn on power saving mode when driving, turn off when stopped"
  mode: single
  trigger:
    # When vehicle starts moving
    - id: start_driving
      trigger: state
      entity_id: binary_sensor.vehicle_is_moving
      to: "on"
      # Wait 30 seconds to avoid toggling at traffic lights
      for: "00:00:30"

    # When vehicle stops moving
    - id: stop_driving
      trigger: state
      entity_id: binary_sensor.vehicle_is_moving
      to: "off"
      # Wait 2 minutes to avoid toggling in stop-and-go traffic
      for: "00:02:00"

  action:
    - choose:
        # Turn ON power saving when driving
        - conditions:
            - condition: trigger
              id: start_driving
            # Only if not already in power saving mode
            - condition: state
              entity_id: input_boolean.power_saving_mode
              state: "off"
          sequence:
            - action: script.power_saving_mode_on

        # Turn OFF power saving when stopped
        - conditions:
            - condition: trigger
              id: stop_driving
            # Only if currently in power saving mode
            - condition: state
              entity_id: input_boolean.power_saving_mode
              state: "on"
          sequence:
            - action: script.power_saving_mode_off

# ────────────────────────────────────────────────────────────────
# Ducking in pulseaudio
# ────────────────────────────────────────────────────────────────
# The following automation is used to setup the ducking module in pulse audio which lowers the volume of music from airplay/audio from PC when HA is playing audio through the VLC media player
- alias: "VBAN audio receiver"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start # fires when HA has finished booting
  action:
    - delay: "00:00:15" # let hassio_audio come online
    - action: shell_command.start_vban_receptor

- alias: Shelly EM – one-second ping updater
  mode: single # don’t queue if it takes longer than a second
  max_exceeded: silent # a 1 Hz poll that occasionally runs long shouldn't spam "Already running"
  trigger:
    - trigger: time_pattern
      seconds: "/1" # fires every 1 s
  action:
    - action: homeassistant.update_entity
      target:
        entity_id: binary_sensor.192_168_10_174 # <- your Ping entity

- alias: Inverter, clear pending when ping changes
  mode: queued
  trigger:
    - trigger: state
      entity_id: binary_sensor.192_168_10_174 # your 1 s-updated Ping entity
  action:
    - action: input_boolean.turn_off
      target: { entity_id: input_boolean.inverter_toggle_pending }

# ───────────────────────────────────────────────
#  Bed lift power – auto-off safety timer (30 s)
# ───────────────────────────────────────────────
- id: bed_power_auto_off_30s
  alias: "Bed Power Supply • auto-off after 30 s"
  mode: restart # restarts the 30 s countdown every time the switch is (re)turned on
  trigger:
    # Start / restart the countdown whenever the bed-power switch turns ON
    - trigger: state
      entity_id: switch.a32_pro_switch27_bed_power_supply
      to: "on"
  action:
    # Wait exactly 30 s
    - delay: "00:00:30"
    # Cut power to the bed motor
    - action: switch.turn_off
      target:
        entity_id: switch.a32_pro_switch27_bed_power_supply

- id: keep_home_zone_on_starlink
  alias: Keep Home zone on GPS
  mode: single
  trigger:
    - trigger: time_pattern # every 15 min
      minutes: "/5"
  condition:
    - "{{ state_attr('device_tracker.ublox_gps', 'latitude') not in [none, 'unknown', 'unavailable'] }}"
  action:
    - action: homeassistant.set_location
      data:
        latitude: "{{ state_attr('device_tracker.ublox_gps', 'latitude')  }}"
        longitude: "{{ state_attr('device_tracker.ublox_gps', 'longitude') }}"
        elevation: "{{ (state_attr('device_tracker.ublox_gps', 'altitude') | float(0)) | round(0) | int }}"

# ───────────────────────────────────────────────
# Rolling snapshot of "last active state" @ 1 Hz
# ───────────────────────────────────────────────
# automation: rolling_last_active_snapshot_1hz
- id: rolling_last_active_snapshot_1hz
  alias: "Rolling Snapshot • Last Active State (1 Hz)"
  mode: single
  trigger:
    - trigger: time_pattern
      seconds: "/1"
  condition:
    - condition: state
      entity_id: input_boolean.power_saving_mode
      state: "off"
  action:
    - action: scene.create
      data:
        scene_id: last_active_state
        snapshot_entities:
          - light.led_controller_cct_1
          - light.led_controller_cct_2
          - light.led_controller_sc_1
          - light.led_controller_sc_2
          - switch.a32_pro_do8_switch06_top_monitor
          - switch.a32_pro_do8_switch07_bottom_monitor
          - switch.a32_pro_water_system_master_switch # ← added

# automation: rolling_snapshot_bootstrap_on_start
- id: rolling_snapshot_bootstrap_on_start
  alias: "Rolling Snapshot • Bootstrap on HA start"
  trigger:
    - trigger: homeassistant
      event: start
  condition:
    - condition: state
      entity_id: input_boolean.power_saving_mode
      state: "off"
  action:
    - action: scene.create
      data:
        scene_id: last_active_state
        snapshot_entities:
          - light.led_controller_cct_1
          - light.led_controller_cct_2
          - light.led_controller_sc_1
          - light.led_controller_sc_2
          - switch.a32_pro_do8_switch06_top_monitor
          - switch.a32_pro_do8_switch07_bottom_monitor
          - switch.a32_pro_water_system_master_switch # ← added

# ────────────────────────────────────────────────────────────────
# Shower Mode – on-activation actions
# ────────────────────────────────────────────────────────────────
- id: shower_mode_on_actions
  alias: "Shower Mode – Turn on lights and recirculate"
  mode: single
  trigger:
    - trigger: state
      entity_id: input_boolean.shower_mode
      from: "off"
      to: "on"
  action:
    # Snapshot current shower-related states for later restore
    - action: scene.create
      data:
        scene_id: pre_shower_mode_state
        snapshot_entities:
          - light.shower_lighting_switch
          - light.led_controller_sc_1
          - switch.a32_pro_water_system_master_switch
          - switch.a32_pro_water_system_state_main
          - switch.a32_pro_water_system_state_recirculating_shower

    # Turn shower lights on (linked via blueprint with controller)
    - action: light.turn_on
      target:
        entity_id:
          - light.shower_lighting_switch
          - light.led_controller_sc_1
      data:
        brightness_pct: 100

    # Ensure water system master is ON
    - action: switch.turn_on
      target:
        entity_id: switch.a32_pro_water_system_master_switch

    # Small delay to let master switch settle
    - delay: "00:00:01"

    # Set water system state to Recirculating Shower
    - action: switch.turn_on
      target:
        entity_id: switch.a32_pro_water_system_state_recirculating_shower

    # Open roof fan lid (needed for exhaust)
    - action: cover.open_cover
      target:
        entity_id: cover.ag_pro_roof_fan_lid

    # Set roof fan to exhaust and 60%
    - action: fan.set_direction
      data:
        entity_id: fan.ag_pro_roof_fan
        direction: forward # exhaust
    - action: fan.turn_on
      data:
        entity_id: fan.ag_pro_roof_fan
        percentage: 60

# ────────────────────────────────────────────────────────────────
# Shower Mode – off restores previous state
# ────────────────────────────────────────────────────────────────
- id: shower_mode_off_restore
  alias: "Shower Mode – Restore previous state on OFF"
  mode: single
  trigger:
    - trigger: state
      entity_id: input_boolean.shower_mode
      from: "on"
      to: "off"
  action:
    - action: scene.turn_on
      target:
        entity_id: scene.pre_shower_mode_state

# ────────────────────────────────────────────────────────────────
# Sleep Mode – pre-bed wind-down (dim now, all lights off after 5 min)
#   Triggered by input_boolean.sleep_mode so the dashboard toggle,
#   voice "goodnight", and script.sleep_mode_on all run this routine.
#   Cancelling within the 5 min aborts the shutdown and restores the
#   pre-sleep state (see sleep_mode_off_restore).
# ────────────────────────────────────────────────────────────────
- id: sleep_mode_on_actions
  alias: "Sleep Mode – Pre-bed wind-down on toggle ON"
  # restart: re-pressing (off→on) starts a fresh wind-down with a new snapshot
  mode: restart
  trigger:
    - trigger: state
      entity_id: input_boolean.sleep_mode
      from: "off"
      to: "on"
  action:
    # New wind-down cycle: clear the "completed" flag (so an OFF during the
    # next 5 min is treated as a cancel, not a morning wake).
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.sleep_winddown_complete

    # Snapshot current LIGHT states so we can restore them on cancel
    - action: scene.create
      data:
        scene_id: pre_sleep_mode_lights
        # LED controllers ONLY — do not snapshot the physical switches.
        # Restoring controller + mirrored switch together made the linked-
        # entities blueprint cross-fire and clobber brightness on restore.
        # The switches follow their controllers via the blueprint.
        snapshot_entities:
          - light.led_controller_cct_1
          - light.led_controller_cct_2
          - light.led_controller_sc_1
          - light.led_controller_sc_2

    # Snapshot current monitor states so we can restore them on cancel
    - action: scene.create
      data:
        scene_id: pre_sleep_mode_monitors
        snapshot_entities:
          - switch.a32_pro_do8_switch06_top_monitor
          - switch.a32_pro_do8_switch07_bottom_monitor

    # Turn off both monitors
    - action: switch.turn_off
      target:
        entity_id:
          - switch.a32_pro_do8_switch06_top_monitor
          - switch.a32_pro_do8_switch07_bottom_monitor

    # Wind-down lighting: under-cabinet 70 %, skylight (Accent) 40 %, main 10 %
    - action: light.turn_on
      target:
        entity_id: light.led_controller_cct_2   # under-cabinet
      data:
        brightness_pct: 70
    - action: light.turn_on
      target:
        entity_id: light.led_controller_sc_2    # skylight (Accent)
      data:
        brightness_pct: 40
    - action: light.turn_on
      target:
        entity_id: light.led_controller_cct_1   # main
      data:
        brightness_pct: 10

    # Turn every other interior light off (shower)
    - action: light.turn_off
      target:
        entity_id: light.led_controller_sc_1    # shower

    # Schedule Starlink sleep start (+30 min). continue_on_error so a Starlink
    # hiccup (e.g. time entity unavailable) can never abort the wind-down
    # before the 5-min auto-off below.
    - action: script.starlink_sleep_start_after_30m
      continue_on_error: true

    # Set Starlink sleep END to 08:15 MST equivalent in current local time
    - continue_on_error: true
      choose:
        - conditions:
            - condition: template
              value_template: "{{ states('time.starlink_sleep_end') not in ['unknown', 'unavailable'] }}"
          sequence:
            - action: time.set_value
              target:
                entity_id: time.starlink_sleep_end
              data:
                time: >
                  {% set mst_offset = -7 %}  {# MST fixed UTC-7 #}
                  {% set local_offset = (now().utcoffset().total_seconds() / 3600) | int %}
                  {% set delta = local_offset - mst_offset %}
                  {% set hour = (8 + delta) % 24 %}
                  {{ '%02d:%02d:%02d' % (hour, 15, 0) }}

    # ~5 min for the nightly routine, then turn everything off — but only
    # if sleep mode wasn't cancelled (which would have restored state).
    - delay: "00:05:00"
    - if:
        - condition: state
          entity_id: input_boolean.sleep_mode
          state: "on"
      then:
        - action: light.turn_off
          target:
            entity_id:
              - light.led_controller_cct_1
              - light.led_controller_cct_2
              - light.led_controller_sc_1
              - light.led_controller_sc_2
              - light.main_lighting_switch
              - light.under_cabinet_lighting_switch
        # Wind-down finished → mark as asleep so the next OFF runs the
        # morning wake-up routine instead of a plain restore.
        - action: input_boolean.turn_on
          target:
            entity_id: input_boolean.sleep_winddown_complete

# ────────────────────────────────────────────────────────────────
# Sleep Mode – cancel (restore) vs morning wake (toggle OFF)
#   Branches on input_boolean.sleep_winddown_complete:
#     • cancelled mid wind-down      → just restore the pre-sleep state
#     • wind-down completed (asleep) → full morning wake-up routine
#   Always clears the flag and resets the Starlink schedule (1 AM MST).
# ────────────────────────────────────────────────────────────────
- id: sleep_mode_off_restore
  alias: "Sleep Mode – Cancel restore / morning wake on toggle OFF"
  mode: restart
  trigger:
    - trigger: state
      entity_id: input_boolean.sleep_mode
      from: "on"
      to: "off"
  action:
    - choose:
        # ── Morning wake: the wind-down completed (lights had auto-off'd) ──
        - conditions:
            - condition: state
              entity_id: input_boolean.sleep_winddown_complete
              state: "on"
          sequence:
            # Ensure inverter is ON before restoring anything AC-dependent
            - action: script.inverter_ensure_on

            # Restore pre-sleep LIGHT states
            - action: scene.turn_on
              target:
                entity_id: scene.pre_sleep_mode_lights

            # Turn water system back ON to Main
            - action: switch.turn_on
              target:
                entity_id: switch.a32_pro_water_system_master_switch
            - action: switch.turn_on
              target:
                entity_id: switch.a32_pro_water_system_state_main

            # Restore monitors to their pre-sleep state
            - action: scene.turn_on
              target:
                entity_id: scene.pre_sleep_mode_monitors

            # Gentle lights: under-cabinet to 30 % for a soft wake
            - action: light.turn_on
              target:
                entity_id: light.under_cabinet_lighting_switch
              data:
                brightness_pct: 30

            # Optionally restore last active scene if not in power-saving
            - choose:
                - conditions:
                    - condition: state
                      entity_id: input_boolean.power_saving_mode
                      state: "off"
                  sequence:
                    - action: scene.turn_on
                      target:
                        entity_id: scene.last_active_state
      # ── Cancel: turned off mid wind-down → just restore prior state ──
      default:
        - action: scene.turn_on
          target:
            entity_id:
              - scene.pre_sleep_mode_lights
              - scene.pre_sleep_mode_monitors

    # Clear the wind-down flag for the next cycle
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.sleep_winddown_complete

    # Revert Starlink sleep start to the default nightly window
    # (1 AM MST), undoing the +30 min override set on sleep-on.
    - action: script.starlink_sleep_start_default
      continue_on_error: true

# ───────────────────────────────────────────────
#  Heater low-fuel lockout – auto-clear when fuel recovers
# ───────────────────────────────────────────────
# When the stable fuel level rises above 30% (hysteresis above
# the 25% lockout threshold), automatically clear the lockout
# so the heater can be used again without manual intervention.

- id: heater_fuel_lockout_auto_clear
  alias: "Heater – Clear low fuel lockout when fuel > 30%"
  mode: single
  trigger:
    - trigger: numeric_state
      entity_id: sensor.stable_fuel_level
      above: 30
      for:
        minutes: 2
  condition:
    - condition: state
      entity_id: input_boolean.heater_low_fuel_lockout
      state: "on"
  action:
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.heater_low_fuel_lockout
    - action: system_log.write
      data:
        message: "Heater low fuel lockout cleared – fuel above 30%."
        level: info

- id: osrm_proxy_start_on_boot
  alias: "OSRM Proxy • Start on HA boot"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start
  action:
    - delay: "00:00:35"
    - action: shell_command.start_osrm_proxy

- id: gps_ublox_start_on_boot
  alias: "GPS u-blox • Start on HA boot"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start
  action:
    - delay: "00:00:40"
    - action: shell_command.start_gps_ublox

- id: gps_filter_start_on_boot
  alias: "GPS Filter • Start on HA boot"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start
  action:
    - delay: "00:00:45"
    - action: shell_command.start_gps_filter

- id: dvr_proxy_start_on_boot
  alias: "DVR Proxy • Start on HA boot"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start
  action:
    - delay: "00:00:40"
    - action: shell_command.start_dvr_proxy

# ───────────────────────────────────────────────
#  Shelly EM – reconnect when inverter turns on
# ───────────────────────────────────────────────
# The Shelly EM is powered by the inverter's AC output.
# When the inverter is off, the Shelly is unreachable and
# HA puts the integration into a retry backoff. When the
# ping sensor detects the Shelly is back, force a config
# entry reload so entities become available immediately.
- id: shelly_em_reconnect_on_inverter
  alias: "Shelly EM • Reconnect when inverter turns on"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.192_168_10_174
      from: "off"
      to: "on"
      for: "00:00:05"
  action:
    - action: homeassistant.reload_config_entry
      data:
        entry_id: 01JZ9P031ZD8WK711KS3ER124D

# ───────────────────────────────────────────────
#  Inverter state tracking (input_boolean.inverter_state)
# ───────────────────────────────────────────────
# Uses the fast ping sensor (1s updates) to maintain a
# stored on/off state for the inverter. The frontend reads
# this instead of the slow Shelly voltage sensor (~40s lag).
- id: inverter_state_track_on
  alias: "Inverter State • Track ON"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.192_168_10_174
      from: "off"
      to: "on"
      for: "00:00:03"
  action:
    - action: input_boolean.turn_on
      target:
        entity_id: input_boolean.inverter_state

- id: inverter_state_track_off
  alias: "Inverter State • Track OFF"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.192_168_10_174
      from: "on"
      to: "off"
      for: "00:00:03"
  action:
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.inverter_state

# ───────────────────────────────────────────────
#  Inverter manual lock (respect a manual off)
# ───────────────────────────────────────────────
# When YOU press the inverter button, remember the direction you chose so the
# power-saving / wake "ensure on" flows don't fight you:
#   • you turned it OFF  → set the lock  → ensure_on is suppressed
#   • you turned it ON   → clear the lock → auto behavior resumes
# Script/automation presses run without a user context, so they're ignored.
- id: inverter_manual_lock_manager
  alias: "Inverter • Manual lock follows user press"
  mode: queued
  max: 5
  trigger:
    - trigger: state
      entity_id: button.a32_pro_inverter_on_off_toggle
  condition:
    # Only react to a DIRECT user press of the inverter button: a real user
    # (user_id set) and not a press cascaded from a script/automation
    # (parent_id is None). Scripts pressing the button never satisfy both.
    - "{{ trigger.to_state is not none
          and trigger.to_state.context.user_id is not none
          and trigger.to_state.context.parent_id is none }}"
  action:
    # Let the 1 s ping → inverter_state settle after the toggle takes effect.
    - delay: "00:00:10"
    - if:
        - condition: state
          entity_id: input_boolean.inverter_state
          state: "off"
      then:
        - action: input_boolean.turn_on
          target:
            entity_id: input_boolean.inverter_manual_lock
      else:
        - action: input_boolean.turn_off
          target:
            entity_id: input_boolean.inverter_manual_lock

# ────────────────────────────────────────────────────────────────
# WiCAN OBD — PID Logging on Engine Start (native integration)
# ────────────────────────────────────────────────────────────────
- id: wican_drive_start_log
  alias: "WiCAN • Log PIDs on engine start"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.engine_is_running
      from: "off"
      to: "on"
      for: "00:00:10"
  action:
    - action: system_log.write
      data:
        level: info
        logger: automation.wican_drive_log
        message: >
          === DRIVE START — PID Snapshot ===
          Time: {{ now().isoformat() }}
          Speed: {{ states('sensor.192_168_10_90_0d_vehiclespeed') }} km/h
          RPM: {{ states('sensor.192_168_10_90_0c_enginerpm') }}
          Fuel: {{ states('sensor.192_168_10_90_2f_fueltanklevel') }}%
          Coolant: {{ states('sensor.192_168_10_90_05_enginecoolanttemp') }}°C
          Throttle: {{ states('sensor.192_168_10_90_11_throttleposition') }}%
          Load: {{ states('sensor.192_168_10_90_04_calcengineload') }}%
          ECU Volt: {{ states('sensor.192_168_10_90_42_controlmodulevolt') }} V
          Ambient: {{ states('sensor.192_168_10_90_46_ambientairtemp') }}°C
          Tires FL/FR/RL/RR: {{ states('sensor.192_168_10_90_tyre_p_fl') }}/{{ states('sensor.192_168_10_90_tyre_p_fr') }}/{{ states('sensor.192_168_10_90_tyre_p_rl') }}/{{ states('sensor.192_168_10_90_tyre_p_rr') }} kPa
          Trans: {{ states('sensor.192_168_10_90_tran_f_temp') }}°C
          Gear: {{ states('sensor.192_168_10_90_gear') }}
          Oil Life: {{ states('sensor.192_168_10_90_oil_life') }}%
          Wastegate: {{ states('sensor.192_168_10_90_wastegate') }}%
          Intake Air: {{ states('sensor.192_168_10_90_intake_air_tmp') }}°C
          Fuel Pressure: {{ states('sensor.192_168_10_90_fuel_pressure') }} kPa
          DTC Count: {{ states('sensor.dtc_count') }} | CEL: {{ states('binary_sensor.check_engine_light') }}
    - action: persistent_notification.create
      data:
        title: "Drive Start — OBD Snapshot {{ now().strftime('%H:%M') }}"
        message: >
          Speed: {{ states('sensor.192_168_10_90_0d_vehiclespeed') }} km/h | RPM: {{ states('sensor.192_168_10_90_0c_enginerpm') }}
          Fuel: {{ states('sensor.192_168_10_90_2f_fueltanklevel') }}% | Coolant: {{ states('sensor.192_168_10_90_05_enginecoolanttemp') }}°C
          Throttle: {{ states('sensor.192_168_10_90_11_throttleposition') }}% | Load: {{ states('sensor.192_168_10_90_04_calcengineload') }}%
          Tires FL/FR/RL/RR: {{ states('sensor.192_168_10_90_tyre_p_fl') }}/{{ states('sensor.192_168_10_90_tyre_p_fr') }}/{{ states('sensor.192_168_10_90_tyre_p_rl') }}/{{ states('sensor.192_168_10_90_tyre_p_rr') }} kPa
          Trans: {{ states('sensor.192_168_10_90_tran_f_temp') }}°C | Gear: {{ states('sensor.192_168_10_90_gear') }}
          Oil: {{ states('sensor.192_168_10_90_oil_life') }}% | Wastegate: {{ states('sensor.192_168_10_90_wastegate') }}%
          DTC: {{ states('sensor.dtc_count') }} | CEL: {{ states('binary_sensor.check_engine_light') }}
        notification_id: wican_drive_log

# Periodic PID log every 5 minutes during a drive
- id: wican_drive_periodic_log
  alias: "WiCAN • Periodic PID log while driving"
  mode: single
  trigger:
    - trigger: time_pattern
      minutes: "/5"
  condition:
    - condition: state
      entity_id: binary_sensor.engine_is_running
      state: "on"
  action:
    - action: system_log.write
      data:
        level: info
        logger: automation.wican_drive_log
        message: >
          === PERIODIC PID LOG ===
          Time: {{ now().isoformat() }}
          Speed: {{ states('sensor.192_168_10_90_0d_vehiclespeed') }} | RPM: {{ states('sensor.192_168_10_90_0c_enginerpm') }} | Fuel: {{ states('sensor.192_168_10_90_2f_fueltanklevel') }}%
          Coolant: {{ states('sensor.192_168_10_90_05_enginecoolanttemp') }}°C | Trans: {{ states('sensor.192_168_10_90_tran_f_temp') }}°C
          Tires FL/FR/RL/RR: {{ states('sensor.192_168_10_90_tyre_p_fl') }}/{{ states('sensor.192_168_10_90_tyre_p_fr') }}/{{ states('sensor.192_168_10_90_tyre_p_rl') }}/{{ states('sensor.192_168_10_90_tyre_p_rr') }} kPa

# ────────────────────────────────────────────────────────────────
# Host Python daemons — liveness watchdog
# ────────────────────────────────────────────────────────────────
# The osrm_proxy / gps_ublox / gps_filter / dvr_proxy daemons run inside the HA
# Core container and are otherwise only (re)started on full HA boot. When one
# crashes mid-run (e.g. osrm_proxy + gps_filter both found dead 2026-06-23,
# gps_filter down since ~06-22) nothing relaunches it and the vanlife map goes
# blank until the next restart.
#
# This re-runs the idempotent start_* commands every 3 min. Each one self-guards
# (port nc-check for the proxies, /tmp pid-file + kill -0 for the gps daemons),
# so it's a no-op when the daemon is alive and a clean relaunch when it's dead —
# crash recovery within ~3 min instead of waiting for a reboot. When a daemon was
# actually dead its start command prints "...started" (not "already running"), so
# we log a warning naming it: a crash trail for root-causing repeat offenders.
- id: daemons_liveness_watchdog
  alias: "Daemons • Liveness watchdog"
  initial_state: true  # always armed after a restart, regardless of restored state
  mode: single
  max_exceeded: silent
  trigger:
    - trigger: time_pattern
      minutes: "/3"
  action:
    - action: shell_command.start_osrm_proxy
      response_variable: osrm_r
    - action: shell_command.start_gps_ublox
      response_variable: ublox_r
    - action: shell_command.start_gps_filter
      response_variable: filter_r
    - action: shell_command.start_dvr_proxy
      response_variable: dvr_r
    # Each start_* echoes one of three things: "...already running" (was alive,
    # no-op), "...started" (was dead → relaunched: the crash event we want a
    # trail of), or an error with neither (start was attempted but not confirmed,
    # e.g. gps_filter with a missing token). Split those so the warning labels are
    # accurate instead of calling a failed start a "relaunch".
    - variables:
        relaunched: >-
          {% set ns = namespace(d=[]) %}
          {% if 'started' in (osrm_r.stdout | default('')) %}{% set ns.d = ns.d + ['osrm_proxy (8765)'] %}{% endif %}
          {% if 'started' in (ublox_r.stdout | default('')) %}{% set ns.d = ns.d + ['gps_ublox'] %}{% endif %}
          {% if 'started' in (filter_r.stdout | default('')) %}{% set ns.d = ns.d + ['gps_filter'] %}{% endif %}
          {% if 'started' in (dvr_r.stdout | default('')) %}{% set ns.d = ns.d + ['dvr_proxy (8766)'] %}{% endif %}
          {{ ns.d | join(', ') }}
        failed: >-
          {% set ns = namespace(d=[]) %}
          {% set o = osrm_r.stdout | default('') %}{% if 'already running' not in o and 'started' not in o %}{% set ns.d = ns.d + ['osrm_proxy (8765)'] %}{% endif %}
          {% set u = ublox_r.stdout | default('') %}{% if 'already running' not in u and 'started' not in u %}{% set ns.d = ns.d + ['gps_ublox'] %}{% endif %}
          {% set f = filter_r.stdout | default('') %}{% if 'already running' not in f and 'started' not in f %}{% set ns.d = ns.d + ['gps_filter'] %}{% endif %}
          {% set v = dvr_r.stdout | default('') %}{% if 'already running' not in v and 'started' not in v %}{% set ns.d = ns.d + ['dvr_proxy (8766)'] %}{% endif %}
          {{ ns.d | join(', ') }}
    # Relaunched a dead daemon → warning (the crash trail for root-causing).
    - if: "{{ relaunched | trim | length > 0 }}"
      then:
        - action: system_log.write
          data:
            level: warning
            logger: watchdog.daemons
            message: "Relaunched dead host daemon(s): {{ relaunched | trim }}"
    # Start attempted but daemon never confirmed up → error (won't self-heal).
    - if: "{{ failed | trim | length > 0 }}"
      then:
        - action: system_log.write
          data:
            level: error
            logger: watchdog.daemons
            message: "Host daemon(s) failed to start (check their .log): {{ failed | trim }}"

# ────────────────────────────────────────────────────────────────
# Ready-to-Drive Guard — warn on camp items still active at departure
# ────────────────────────────────────────────────────────────────
# When the van heads toward driving (engine start / park-brake release / motion),
# if any "hard" camp-teardown item is still active (sensor.rtd_offenders > 0),
# raise a persistent_notification listing the offenders. A fixed notification_id
# means the 3 triggers update ONE card instead of stacking — so no de-nag helper
# is needed. Suppressed while input_boolean.rtd_guard_override ("I know") is on.
# The React "Ready to Drive" banner (Home page) is the always-live visual; this
# automation is the notification so you're warned even without the dashboard open.
- id: rtd_guard_warn
  alias: "Ready-to-Drive Guard • warn on camp items"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.engine_is_running
      from: "off"
      to: "on"
      for: "00:00:05"          # debounce the start-up RPM flicker
    - trigger: state
      entity_id: sensor.192_168_10_90_park_brake
      to: "0"                  # park brake released
    - trigger: state
      entity_id: binary_sensor.vehicle_is_moving
      from: "off"
      to: "on"                 # last-ditch net
  condition:
    - condition: state
      entity_id: input_boolean.rtd_guard_override
      state: "off"
    - condition: state
      entity_id: binary_sensor.rtd_needs_attention
      state: "on"
  action:
    - action: persistent_notification.create
      data:
        notification_id: rtd_guard
        title: "⚠ Not ready to drive — {{ states('sensor.rtd_offenders') }} item(s)"
        message: "• {{ state_attr('sensor.rtd_offenders', 'summary') }}"

# Clear the warning automatically once everything is stowed/off.
- id: rtd_guard_clear
  alias: "Ready-to-Drive Guard • dismiss when clear"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.rtd_needs_attention
      # A template reload flips the sensor on → unavailable → off → on within
      # a second (seen 2026-09-14 and 09-16). Without `from`, that dismissed
      # the warning while items were still out.
      from: "on"
      to: "off"
  action:
    - action: persistent_notification.dismiss
      data:
        notification_id: rtd_guard

# "I know" only lasts the current drive — clear the override when the engine stops.
- id: rtd_guard_override_autoclear
  alias: "Ready-to-Drive Guard • clear override on engine off"
  mode: single
  trigger:
    - trigger: state
      entity_id: binary_sensor.engine_is_running
      from: "on"
      to: "off"
      for: "00:00:30"
  condition:
    - condition: state
      entity_id: input_boolean.rtd_guard_override
      state: "on"
  action:
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.rtd_guard_override

# ────────────────────────────────────────────────────────────────
# NWS severe-weather alert — notify on a NEW Severe/Extreme alert at the van
# ────────────────────────────────────────────────────────────────
# sensor.nws_highest_alert (rest, resource_template = live GPS) tracks the
# highest active NWS alert at the van's location. Fire a persistent_notification
# only when a Severe/Extreme alert appears whose id we haven't already notified
# (input_text.nws_last_notified_id), so a 5-min re-poll of the same alert stays
# silent. persistent_notification is the only delivery configured (no mobile push).
- id: nws_severe_alert_notify
  alias: "NWS • notify on new severe alert at van location"
  mode: single
  trigger:
    - trigger: state
      entity_id: sensor.nws_highest_alert
  condition:
    - condition: template
      value_template: "{{ states('sensor.nws_highest_alert') not in ['none','unknown','unavailable',''] }}"
    - condition: template
      value_template: "{{ state_attr('sensor.nws_highest_alert','highest_severity') in ['Extreme','Severe'] }}"
    - condition: template
      value_template: "{{ state_attr('sensor.nws_highest_alert','alert_id') != states('input_text.nws_last_notified_id') }}"
  action:
    - action: persistent_notification.create
      data:
        notification_id: nws_alert
        title: "⚠️ {{ states('sensor.nws_highest_alert') }}"
        message: >
          {{ state_attr('sensor.nws_highest_alert','headline')
             or 'Severe weather alert active at your location.' }}
    - action: input_text.set_value
      target:
        entity_id: input_text.nws_last_notified_id
      data:
        value: "{{ state_attr('sensor.nws_highest_alert','alert_id') }}"


# ────────────────────────────────────────────────────────────────
# Starlink • auto-recover from the PoE DC-DC converter lockup
# ────────────────────────────────────────────────────────────────
# While driving, the DC-DC converter feeding the Starlink PoE injector sometimes
# latches up: the dish goes completely unreachable and stays that way until the
# injector is fully de-energised long enough to drain (or the ethernet is pulled
# at the dish end). See script.starlink_recover for the timing rationale.
#
# The 15-minute debounce is set from 45 days of history, not taste. Short
# `unavailable` blips are extremely common and self-heal — 789 spans under a
# minute, ~36 more under 15 minutes. Everything that actually needed hands ran
# far longer (2026-08-07: 91 min; 2026-08-06: 242 min; 2026-07-18: 230 min), so
# 15 min cleanly separates "real lockup" from "integration hiccup" while still
# cutting today's outage from 91 minutes down to about 17.
#
# A template trigger (not a state trigger) so it also re-arms correctly if HA
# restarts or reloads while the dish is already dark — a state trigger would
# need a transition that has already happened and would never fire.
- id: starlink_auto_recover
  alias: "Starlink • auto power-cycle when the link drops"
  mode: single
  trigger:
    - trigger: template
      value_template: >
        {{ states('binary_sensor.starlink_connectivity') in ['unavailable', 'unknown'] }}
      for: "00:15:00"
  condition:
    - condition: state
      entity_id: input_boolean.starlink_auto_recovery
      state: "on"
    # Never fight a deliberate manual cut — if YOU switched the supply off, the
    # dish being unreachable is the intended outcome, not a fault.
    - condition: state
      entity_id: switch.a32_pro_switch26_starlink_power_supply
      state: "on"
    - condition: state
      entity_id: input_boolean.starlink_recovery_in_progress
      state: "off"
    # Already tried twice and gave up — don't cycle the supply forever.
    # Cleared by starlink_recovery_reset once the link genuinely returns.
    - condition: state
      entity_id: input_boolean.starlink_recovery_failed
      state: "off"
  action:
    - action: script.turn_on
      target:
        entity_id: script.starlink_recover

# Link is genuinely back → clear the latch so the banner disappears and the next
# lockup gets a fresh pair of attempts. The 5-minute hold stops a dish that is
# flapping in and out from re-arming the retries every few seconds.
- id: starlink_recovery_reset
  alias: "Starlink • clear recovery latch when the link returns"
  mode: single
  trigger:
    - trigger: template
      value_template: >
        {{ states('binary_sensor.starlink_connectivity') not in ['unavailable', 'unknown'] }}
      for: "00:05:00"
  condition:
    - condition: state
      entity_id: input_boolean.starlink_recovery_in_progress
      state: "off"
    # Only act when there is actually something to clear.
    - condition: template
      value_template: >
        {{ is_state('input_boolean.starlink_recovery_failed', 'on')
           or states('input_number.starlink_recovery_attempts') | int(0) > 0 }}
  action:
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.starlink_recovery_failed
    - action: input_number.set_value
      target:
        entity_id: input_number.starlink_recovery_attempts
      data:
        value: 0
    - action: persistent_notification.dismiss
      data:
        notification_id: starlink_recovery

# Fail-safe: never leave the dish de-energised because HA restarted mid-cycle.
# script.starlink_recover holds the supply OFF for 90 s. A restart (or a crash)
# inside that window kills the script without its turn_on step ever running, and
# the ESPHome switch's RESTORE_DEFAULT_ON only helps if the ESP itself reboots —
# not if HA does. in_progress still being ON at startup is exactly that
# signature, so re-energise and clear the flag.
- id: starlink_recovery_resume_after_restart
  alias: "Starlink • re-energise supply if a restart interrupted recovery"
  mode: single
  trigger:
    - trigger: homeassistant
      event: start
  condition:
    - condition: state
      entity_id: input_boolean.starlink_recovery_in_progress
      state: "on"
  action:
    - action: switch.turn_on
      target:
        entity_id: switch.a32_pro_switch26_starlink_power_supply
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.starlink_recovery_in_progress
    - action: persistent_notification.create
      data:
        notification_id: starlink_recovery
        title: "🛰 Starlink recovery interrupted"
        message: >
          Home Assistant restarted while the Starlink power supply was cut. The
          supply has been switched back on. If the dish is still offline, the
          recovery will start again on its own in about 15 minutes.

# ════════════════════════════════════════════════════════════════
#  SHOP MODE — hands-off lockout while the van is at a shop
# ════════════════════════════════════════════════════════════════
# Arm by toggling input_boolean.shop_mode (dashboard, voice, or
# script.shop_mode_enter). What the mode actually does lives in
# script.shop_mode_apply — these automations only drive it.
#
# What this CANNOT do, and you should plan around:
#   • The physical rocker switches are wired to the a32_pro directly. HA
#     turning a light "off" does not stop someone flipping the rocker.
#   • It cannot stop the shop pulling the battery disconnect — which is the
#     standard advice for shop visits anyway, and is strictly safer than
#     anything HA can do in software.
# ════════════════════════════════════════════════════════════════

- id: shop_mode_on_actions
  alias: "Shop Mode • arm lockout"
  mode: single
  trigger:
    - trigger: state
      entity_id: input_boolean.shop_mode
      from: "off"
      to: "on"
  action:
    # Record which automations were ALREADY disabled before arming, so exit can
    # put things back exactly as they were instead of blanket-enabling. Written
    # BEFORE script.shop_mode_apply disables everything, or the list would just
    # be "all of them". 'OVERFLOW' means it did not fit the 255-char input_text
    # cap, and exit falls back to enabling everything.
    - action: input_text.set_value
      target:
        entity_id: input_text.shop_mode_prev_disabled
      data:
        value: >-
          {% set off = states.automation | selectattr('state', 'eq', 'off')
               | map(attribute='entity_id') | map('replace', 'automation.', '')
               | list %}
          {% set j = off | join(',') %}
          {{ 'none' if off | count == 0 else (j if j | length <= 255 else 'OVERFLOW') }}

    # Snapshot ONLY the comfort items we are willing to bring back automatically
    # on exit. Gas, water, heat, bed lift and the compressor are deliberately
    # excluded — those get turned back on by hand, deliberately, not by a toggle
    # tapped in a shop parking lot.
    - action: scene.create
      data:
        scene_id: pre_shop_mode_state
        snapshot_entities:
          - light.led_controller_cct_1
          - light.led_controller_cct_2
          - light.led_controller_sc_1
          - light.led_controller_sc_2
          - light.main_lighting_switch
          - light.under_cabinet_lighting_switch
          - switch.a32_pro_do8_switch06_top_monitor
          - switch.a32_pro_do8_switch07_bottom_monitor

    - action: script.shop_mode_apply

    - action: persistent_notification.create
      data:
        notification_id: shop_mode
        title: "🔧 Shop Mode ARMED"
        message: >-
          Automations disabled, actuators forced off. LPG valve closed, hydronic
          heater off, water system off, bed lift dead, air compressor off, roof
          lid closed, inverter off + locked.


          Still live: GPS tracking, BMS/battery monitoring, Starlink (remote
          access). Trip + VE logging is suppressed, so shop test drives will NOT
          pollute your fuel data.

          {% if states('input_text.shop_mode_prev_disabled') == 'OVERFLOW' %}

          WARNING: too many automations were already disabled to record them
          all — disarming will enable ALL automations. Re-check any you had off.
          {% endif %}


          The physical rockers still work, and the battery disconnect is still
          the real lockout.

- id: shop_mode_off_restore
  alias: "Shop Mode • disarm and restore"
  mode: single
  trigger:
    - trigger: state
      entity_id: input_boolean.shop_mode
      from: "on"
      to: "off"
  action:
    - variables:
        prev: "{{ states('input_text.shop_mode_prev_disabled') }}"

    # Release the inverter so script.inverter_ensure_on works normally again.
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.inverter_manual_lock

    # Re-energise the hydronic heater's power supply. Switch32 is the heater's
    # STANDBY supply, not the heater itself: it is normally always on and
    # script.shop_mode_apply is the only thing that ever cuts it. While it is
    # off the a32_pro refuses to turn Switch24 on at all — it logs "Cannot turn
    # on hydronic heater: Power Supply is OFF!" and does nothing — and nothing
    # on the dashboard exposed Switch32, so after the 2026-08-22 shop visit the
    # heater looked dead for two weeks (2026-09-06: thermostat calling for
    # heat at 10.9 °C, heater "Idle.", dashboard switch snapping back off).
    # The heater enable (Switch24) and its thermostat stay OFF as before —
    # restoring the supply is what makes turning them back on by hand possible.
    - action: switch.turn_on
      target:
        entity_id: switch.a32_pro_switch32_hydronic_heater_power_supply

    # Bring back lights/monitors only if the snapshot survived. scene.create
    # scenes are in-memory, so after a reboot at the shop this is simply gone —
    # that is expected, not an error.
    #
    # ORDER MATTERS: this runs BEFORE automations are re-armed. Re-arming first
    # would restart rolling_last_active_snapshot_1hz, which would capture the
    # still-all-off state into scene.last_active_state within 1 s and blow away
    # the Power Saving Mode restore point. Restoring the lights first means the
    # rolling snapshot only ever sees the good state.
    - if:
        - condition: template
          value_template: "{{ states.scene.pre_shop_mode_state is not none }}"
      then:
        - action: scene.turn_on
          target:
            entity_id: scene.pre_shop_mode_state

    # Re-arm everything, then re-disable exactly what was off beforehand.
    # Same ghost filter as script.shop_mode_apply — only real, loaded automations.
    - action: automation.turn_on
      target:
        entity_id: >-
          {{ states.automation
             | selectattr('state', 'in', ['on', 'off'])
             | map(attribute='entity_id') | list }}

    - if:
        - condition: template
          value_template: >-
            {{ prev not in ['none', 'OVERFLOW', 'unknown', 'unavailable', ''] }}
      then:
        - action: automation.turn_off
          data:
            stop_actions: true
          target:
            # Filter against automations that actually exist, so a stale or
            # truncated entry can never abort the whole call.
            entity_id: >-
              {% set live = states.automation | map(attribute='entity_id') | list %}
              {{ prev.split(',') | map('trim')
                 | map('regex_replace', '^', 'automation.')
                 | select('in', live) | list }}

    - action: persistent_notification.dismiss
      data:
        notification_id: shop_mode
    - action: persistent_notification.create
      data:
        notification_id: shop_mode_disarmed
        title: "✅ Shop Mode disarmed"
        message: >-
          Automations re-armed. Lights and monitors restored.


          Still OFF on purpose — turn these back on yourself when you are ready:
          LPG valve, hydronic heater + its thermostat, water system, bed lift
          power, air compressor, shore charger. The heater's power supply is
          back on, so the dashboard heater switch works again.

- id: shop_mode_guard
  alias: "Shop Mode • re-shut anything that comes back on"
  # queued so a burst of entities coming back at once is handled one at a time
  # rather than dropped; silent because that burst is normal, not an error.
  mode: queued
  max: 30
  max_exceeded: silent
  trigger:
    - trigger: state
      id: turned_on
      to: "on"
      # Must stay in sync with script.shop_mode_apply. Every entity that script
      # forces off is watched here, so anything that comes back on — an a32_pro
      # relay reasserting itself, a stray service call — gets re-shut.
      entity_id:
        - switch.a32_pro_switch16_lpg_valve
        - switch.a32_pro_switch24_hydronic_heater
        - switch.a32_pro_switch32_hydronic_heater_power_supply
        - switch.a32_pro_switch27_bed_power_supply
        - switch.a32_pro_switch07_bed_up
        - switch.a32_pro_switch08_bed_down
        - switch.a32_pro_switch28_compressor
        - switch.a32_pro_switch06_grey_water_tank_valve
        - switch.ag_pro_ceiling_fan_mode
        # The three water_system_state_* template switches are deliberately not
        # watched: they are derived from last_water_state and cannot be turned
        # off, so guarding them would fire a no-op on every mode change.
        - switch.a32_pro_water_system_master_switch
        - switch.a32_pro_switch29_shower_system_water_pump
        - switch.a32_pro_switch30_main_system_water_pump
        - switch.a32_pro_switch10_water_system_overall
        - switch.a32_pro_switch11_water_system_state_main
        - switch.a32_pro_switch12_water_system_state_recirculating_shower
        - switch.a32_pro_switch13_water_system_state_recirculating_shower_flush
        - switch.a32_pro_switch01_water_system_valve_1
        - switch.a32_pro_switch02_water_system_valve_2
        - switch.a32_pro_switch03_water_system_valve_3
        - switch.a32_pro_switch14_uv_filter
        - switch.a32_pro_switch17_silicone_heating_pads_shower_water_tank_x2
        - switch.a32_pro_switch18_silicone_heating_pads_fresh_water_tank_x4
        - switch.a32_pro_switch19_silicone_heating_pads_grey_water_tank_x4
        - switch.a32_pro_switch20_heating_pads_auto_thermostat_grey_water_tank_x2
        - switch.a32_pro_fresh_water_tank_heater_enable
        - switch.a32_pro_grey_water_tank_heater_enable
        - switch.a32_pro_shower_water_tank_heater_enable
        - switch.a32_pro_do8_switch04_shore_power_charger
        - switch.a32_pro_do8_switch06_top_monitor
        - switch.a32_pro_do8_switch07_bottom_monitor
        - switch.a32_pro_do8_switch08_air_fryer_ventilation_fans
        - switch.a32_pro_air_fryer_ventilation_enable
        - switch.a32_pro_switch21_left_outdoor_lights
        - switch.a32_pro_switch22_right_outdoor_lights
        - switch.a32_pro_switch23_rear_outdoor_lights
        - switch.a32_pro_switch31_lightbar
        - switch.a32_pro_switch09_shower_button_led
        - fan.ag_pro_roof_fan
        - light.led_controller_cct_1
        - light.led_controller_cct_2
        - light.led_controller_sc_1
        - light.led_controller_sc_2
        - light.main_lighting_switch
        - light.under_cabinet_lighting_switch
        - light.a32_pro_a32_pro_dac_0
        - light.a32_pro_a32_pro_dac_1
        - light.0xa4c138fd668411cd
    - trigger: state
      id: lid_opened
      entity_id: cover.ag_pro_roof_fan_lid
      to: "open"
    # The hydronic PID climate runs ON the a32_pro and re-lights the heater by
    # itself whenever it is in `heat` and the supply is live (its on_state
    # handler re-evaluates on every temperature sample). script.shop_mode_apply
    # turns it off; this catches anything turning it back on mid-visit. Its
    # state is heat/off rather than on/off, so it needs its own trigger.
    - trigger: state
      id: climate_heat
      entity_id: climate.a32_pro_van_hydronic_heating_pid
      to: heat
  condition:
    - condition: state
      entity_id: input_boolean.shop_mode
      state: "on"
  action:
    - choose:
        - conditions:
            - condition: trigger
              id: turned_on
          sequence:
            # homeassistant.turn_off is domain-agnostic, so one call covers
            # switch / light / fan without a per-domain branch.
            - action: homeassistant.turn_off
              target:
                entity_id: "{{ trigger.entity_id }}"
        - conditions:
            - condition: trigger
              id: lid_opened
          sequence:
            - action: cover.close_cover
              target:
                entity_id: cover.ag_pro_roof_fan_lid
        - conditions:
            - condition: trigger
              id: climate_heat
          sequence:
            - action: climate.set_hvac_mode
              target:
                entity_id: climate.a32_pro_van_hydronic_heating_pid
              data:
                hvac_mode: "off"

- id: shop_mode_reassert_on_start
  alias: "Shop Mode • re-assert after HA restart"
  # initial_state: true is the whole point of this automation — without it a
  # restart could restore it in the OFF state, and the van would silently come
  # back to life the first time the shop cycles power to the Pi.
  initial_state: true
  mode: single
  trigger:
    - trigger: homeassistant
      event: start
  condition:
    - condition: state
      entity_id: input_boolean.shop_mode
      state: "on"
  action:
    # Let MQTT / BLE / the a32_pro entities actually come online first —
    # applying against `unavailable` entities would be a silent no-op.
    - delay: "00:01:00"
    - action: script.shop_mode_apply
    - action: persistent_notification.create
      data:
        notification_id: shop_mode
        title: "🔧 Shop Mode re-armed after restart"
        message: >-
          HA restarted while Shop Mode was active. The lockout has been
          re-applied automatically.

# ───────────────────────────────────────────────
#  Shoe dryer – blower off when the timer ends
# ───────────────────────────────────────────────
# Fires on finished AND cancelled so the Stop button takes the same path as
# natural expiry. The ha_start leg is the restart reconciler: a timer that
# EXPIRES while HA is down fires timer.finished during timer setup — before
# automation triggers are armed — so that event is always lost, and with the
# ESP never having rebooted the blower would run at 100% forever. The durable
# input_boolean.shoe_dryer_active flag survives the restart and lets this leg
# finish the job. (restore: true on the timer covers the other case: a restart
# shorter than the time remaining resumes the countdown; the condition below
# sees the timer still active and leaves it alone.)
- id: shoe_dryer_auto_off
  alias: "Shoe Dryer • blower off when timer ends"
  mode: single
  trigger:
    - trigger: event
      id: finished
      event_type: timer.finished
      event_data:
        entity_id: timer.shoe_dryer
    - trigger: event
      id: cancelled
      event_type: timer.cancelled
      event_data:
        entity_id: timer.shoe_dryer
    - trigger: homeassistant
      id: ha_start
      event: start
  condition:
    # Only ever act on a dry that is actually in progress...
    - condition: state
      entity_id: input_boolean.shoe_dryer_active
      state: "on"
    # ...and on the startup leg, only if the timer did NOT survive the
    # restart (still active = the dry legitimately continues, hands off).
    - condition: template
      value_template: "{{ trigger.id != 'ha_start' or is_state('timer.shoe_dryer', 'idle') }}"
  action:
    # After a restart the a32_pro needs time to reconnect — commanding an
    # `unavailable` entity is a silent no-op (same reason
    # shop_mode_reassert_on_start waits a minute before applying).
    - if:
        - condition: trigger
          id: ha_start
      then:
        - wait_template: >-
            {{ has_value('light.a32_pro_a32_pro_dac_0')
               and has_value('climate.a32_pro_van_hydronic_heating_pid') }}
          timeout: "00:05:00"
          continue_on_timeout: true
    - action: light.turn_off
      target:
        entity_id: light.a32_pro_a32_pro_dac_0
      continue_on_error: true
    # Restore heating only if it was on before the dry AND nobody has taken
    # the climate over since we paused it — if the user re-enabled heat
    # mid-dry, their newer intent wins over our stale snapshot.
    - if:
        - condition: state
          entity_id: climate.a32_pro_van_hydronic_heating_pid
          state: "off"
        - condition: state
          entity_id: input_boolean.shoe_dryer_resume_heat
          state: "on"
      then:
        - action: climate.set_temperature
          target:
            entity_id: climate.a32_pro_van_hydronic_heating_pid
          data:
            temperature: >-
              {{ states('input_number.shoe_dryer_prev_setpoint') | float(21) }}
            hvac_mode: heat
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.shoe_dryer_active

# If something re-enables the hydronic climate mid-dry (app, voice, wall
# tablet), its PID re-takes the blower within ~10 s and the dry is dead
# anyway — cancel the timer so the dashboard card stops claiming otherwise.
# The cancel routes through shoe_dryer_auto_off, whose climate-off guard then
# correctly SKIPS the snapshot restore (the user's newer intent wins). This
# can't self-trigger from the start script: that only ever moves the climate
# heat→off, never off→heat.
- id: shoe_dryer_cancel_on_climate_takeover
  alias: "Shoe Dryer • cancel when heating retakes the blower"
  mode: single
  trigger:
    - trigger: state
      entity_id: climate.a32_pro_van_hydronic_heating_pid
      from: "off"
      to: heat
  condition:
    - condition: state
      entity_id: timer.shoe_dryer
      state: active
  action:
    - action: timer.cancel
      target:
        entity_id: timer.shoe_dryer

# ────────────────────────────────────────────────────────────────
# Night Climate – hold a night target, warm up for the wake time
#   Mode (input_select.night_climate_mode) is what runs now:
#     Program       heater when cold; roof fan when warm and cooler outside
#                   (its own thermostat, or HA from the room sensor when that
#                   sits idle); A/C only above "A/C above", on shore power and
#                   not while it is clearly colder outside; night target, then
#                   the wake target from the warm-up
#     Hold          the same, at the hold target, with no end (daytime use)
#     Fan all night / A/C all night / Heater
#                   one appliance until the wake time
#   Sleep Mode ON starts the Program; the wake time ends everything and
#   turns Sleep Mode off (wake_up_routine). The heater is left where the
#   warm-up put it (the old 07:30 scheduler entry is gone since 2026-09-17,
#   the warm-up is the morning heat). Sensors in template/night_climate.yaml.
#   One-way IR is backed up three ways since the night of 2026-09-18: the AG
#   Pro spaces fan and A/C frames, the fan's off is sent twice, and
#   night_climate_fan_watchdog re-sends it if the motor still draws power.
# ────────────────────────────────────────────────────────────────
- id: night_climate_start_on_sleep
  alias: "Night Climate – start with Sleep Mode"
  mode: single
  trigger:
    - trigger: state
      entity_id: input_boolean.sleep_mode
      from: "off"
      to: "on"
  condition:
    # A daytime Hold becomes the night Program at bedtime.
    - "{{ states('input_select.night_climate_mode') in ['Off', 'Hold'] }}"
  action:
    - action: input_select.select_option
      target:
        entity_id: input_select.night_climate_mode
      data:
        option: Program

- id: night_climate_stop_on_sleep_off
  alias: "Night Climate – stop when Sleep Mode ends"
  mode: single
  trigger:
    - trigger: state
      entity_id: input_boolean.sleep_mode
      from: "on"
      to: "off"
  condition:
    - "{{ not is_state('input_select.night_climate_mode', 'Off') }}"
  action:
    - action: input_select.select_option
      target:
        entity_id: input_select.night_climate_mode
      data:
        option: "Off"

# Ending the program stops the fan and the A/C; the heater is deliberately
# left where it is.
- id: night_climate_mode_off
  alias: "Night Climate – fan and A/C off when the program ends"
  mode: single
  trigger:
    - trigger: state
      entity_id: input_select.night_climate_mode
      to: "Off"
  action:
    - action: script.night_climate_actuators_off
    - action: input_boolean.turn_off
      target:
        entity_id: input_boolean.night_climate_fan_by_room

- id: night_climate_wake
  alias: "Night Climate – wake time"
  mode: single
  trigger:
    - trigger: time
      at: input_datetime.night_climate_wake_time
  condition:
    # Hold has no end; the night modes stop here.
    - "{{ states('input_select.night_climate_mode') not in ['Off', 'Hold'] }}"
  action:
    # No heater call here on purpose: the warm-up already put the heater at
    # the wake target, and it stays there until someone changes it.
    - action: input_select.select_option
      target:
        entity_id: input_select.night_climate_mode
      data:
        option: "Off"
    - if:
        - condition: state
          entity_id: input_boolean.sleep_mode
          state: "on"
      then:
        - action: script.wake_up_routine

# The loop. Every 5 min (and on any setting change) hold sensor.night_climate_
# target with what the mode allows. The scripts only send a command when the
# appliance is not already where it should be; the fan and the A/C beep on
# every IR frame, so that is deliberate. The A/C keeps a 30-min dwell between
# on and off (compressor and the beeps); the heater has its own PID.
- id: night_climate_controller
  alias: "Night Climate – controller"
  mode: single
  max_exceeded: silent
  trigger:
    - trigger: time_pattern
      minutes: "/5"
    # Setting changes act at once. `to:` empty = state changes only, not the
    # attribute churn on the target sensor.
    - trigger: state
      entity_id:
        - input_select.night_climate_mode
        - input_select.night_climate_fan_direction
        - input_number.night_climate_fan_speed
        - input_boolean.night_climate_use_heater
        - input_boolean.night_climate_use_ac
        - input_boolean.night_climate_use_fan
        - sensor.night_climate_target
      to:
  condition:
    - "{{ states('input_select.night_climate_mode') not in ['Off', 'unknown', 'unavailable'] }}"
  action:
    - variables:
        mode: "{{ states('input_select.night_climate_mode') }}"
        target: "{{ states('sensor.night_climate_target') | float(15) }}"
        room: "{{ states('sensor.living_space_temperature') | float(none) }}"
        # Charger drew power within the last 3 h (template/night_climate.yaml)
        shore: "{{ is_state('binary_sensor.shore_power_present', 'on') }}"
        cool_above: "{{ states('input_number.night_climate_cool_above') | float(24) }}"
        heater_on: "{{ is_state('climate.a32_pro_van_hydronic_heating_pid', 'heat') }}"
        ac_on: "{{ is_state('climate.ag_pro_24v_air_conditioner', 'cool') }}"
        ac_settled: >-
          {{ states.climate.ag_pro_24v_air_conditioner is none
             or (now() - states.climate.ag_pro_24v_air_conditioner.last_changed).total_seconds() > 1800 }}
        use_heater: "{{ is_state('input_boolean.night_climate_use_heater', 'on') }}"
        use_ac: "{{ is_state('input_boolean.night_climate_use_ac', 'on') }}"
        use_fan: "{{ is_state('input_boolean.night_climate_use_fan', 'on') }}"
        fan_thermo: "{{ is_state('switch.ag_pro_roof_fan_thermostat', 'on') }}"
        # Underneath-van sensor reads ~3.5 °C warm, so this is conservative.
        outdoor: "{{ states('sensor.a32_pro_bme280_4_temperature') | float(none) }}"
        fan_on: "{{ is_state('fan.ag_pro_roof_fan', 'on') }}"
        fan_by_room: "{{ is_state('input_boolean.night_climate_fan_by_room', 'on') }}"
        # Cooler outside: worth starting (1 °C) and worth keeping (0.3 °C)
        fan_ok: "{{ room is not none and outdoor is not none and outdoor < room - 1 }}"
        fan_keep: "{{ room is not none and outdoor is not none and outdoor < room - 0.3 }}"
        # Clearly colder outside and the fan allowed: cooling is the fan's job, not the A/C's
        fan_can_cool: "{{ use_fan and room is not none and outdoor is not none and outdoor < room - 3 }}"
        # 20 min on its own thermostat in a warm room and the motor has not
        # turned for 10 min: that thermostat is not doing the job (2026-09-18:
        # in Intake its sensor sits in the incoming air; 103 s of running all night).
        fan_idle: >-
          {% set run = states.binary_sensor.roof_fan_running %}
          {{ fan_thermo and run is not none and run.state == 'off'
             and (now() - run.last_changed).total_seconds() > 600
             and (now() - states.switch.ag_pro_roof_fan_thermostat.last_changed).total_seconds() > 1200 }}
        ac_wanted: >-
          {{ use_ac and shore and room is not none and room > cool_above and not fan_can_cool }}
        fan_settings:
          direction: "{{ states('input_select.night_climate_fan_direction') }}"
          speed: "{{ states('input_number.night_climate_fan_speed') | int(30) }}"
    - choose:
        # ── Heater: just the thermostat at the target ──
        - conditions: "{{ mode == 'Heater' }}"
          sequence:
            - action: script.night_climate_actuators_off
            - action: script.night_climate_heater_to
              data:
                target: "{{ target }}"

        # ── Fan all night: fixed speed and direction; the heater is left alone ──
        - conditions: "{{ mode == 'Fan all night' }}"
          sequence:
            - if:
                - "{{ ac_on }}"
              then:
                - action: script.night_climate_ac_off
            - action: script.night_climate_fan_on
              data:
                speed: "{{ states('input_number.night_climate_fan_speed') | int(30) }}"
                direction: "{{ states('input_select.night_climate_fan_direction') }}"

        # ── A/C all night: the unit's own thermostat at the target, shore power only ──
        - conditions: "{{ mode == 'A/C all night' }}"
          sequence:
            - if:
                - "{{ is_state('fan.ag_pro_roof_fan', 'on') }}"
              then:
                - action: fan.turn_off
                  target:
                    entity_id: fan.ag_pro_roof_fan
            - if:
                - "{{ shore }}"
              then:
                - action: script.night_climate_ac_on
                  data:
                    target: "{{ target }}"
              else:
                - if:
                    - "{{ ac_on }}"
                  then:
                    - action: script.night_climate_ac_off

        # ── Program / Hold: heater below target−1. Above target+2 the roof fan
        #    cools while it is cooler outside: on its own thermostat first, and
        #    if that sits idle (its sensor is in the incoming air) HA runs the
        #    fan from the room sensor instead. The A/C joins only above the
        #    "A/C above" temperature, on shore power, and never while it is
        #    clearly colder outside (2026-09-18: it was called at 21 °C inside
        #    and 9 °C outside to chase a 15 °C target). Once on it cools to the
        #    target. Fan and A/C never run together. Between the bands whatever
        #    is running holds its own target.
        - conditions: "{{ mode in ['Program', 'Hold'] }}"
          sequence:
            # Clearly over target: the heater has no business running
            - if:
                - "{{ heater_on and room is not none and room > target + 2 }}"
              then:
                - action: climate.set_hvac_mode
                  target:
                    entity_id: climate.a32_pro_van_hydronic_heating_pid
                  data:
                    hvac_mode: "off"
            - choose:
                # Too cold: fan and A/C off, heater to target
                - conditions: "{{ room is not none and use_heater and room < target - 1 }}"
                  sequence:
                    - action: script.night_climate_actuators_off
                    - action: script.night_climate_heater_to
                      data:
                        target: "{{ target }}"

                # A/C running: keep it only while it is still the right tool
                - conditions: "{{ ac_on }}"
                  sequence:
                    - choose:
                        # Shore power gone or the A/C no longer allowed: off now
                        - conditions: "{{ not shore or not use_ac }}"
                          sequence:
                            - action: script.night_climate_ac_off
                        # Back under target, or cold enough outside for the fan:
                        # off once the 30-min dwell is over
                        - conditions: >-
                            {{ ac_settled and (fan_can_cool
                                               or (room is not none and room < target - 0.5)) }}
                          sequence:
                            - action: script.night_climate_ac_off
                      default:
                        - if:
                            - "{{ fan_thermo or fan_on }}"
                          then:
                            - action: script.night_climate_fan_off
                        # Keeps the unit's own target in step (the warm-up raises it)
                        - action: script.night_climate_ac_on
                          data:
                            target: "{{ target }}"

                # Too warm: the A/C when it is really warm and the fan cannot do
                # it, else the roof fan while it is cooler outside
                - conditions: "{{ room is not none and room > target + 2 }}"
                  sequence:
                    - choose:
                        - conditions: "{{ ac_wanted and ac_settled }}"
                          sequence:
                            - if:
                                - "{{ fan_thermo or fan_on }}"
                              then:
                                - action: script.night_climate_fan_off
                            - action: script.night_climate_ac_on
                              data:
                                target: "{{ target }}"
                        - conditions: "{{ use_fan and fan_ok }}"
                          sequence:
                            - choose:
                                # Its own thermostat sat idle: HA runs the fan from
                                # the room sensor for the rest of this program
                                - conditions: "{{ fan_by_room or fan_idle }}"
                                  sequence:
                                    - if:
                                        - "{{ not fan_by_room }}"
                                      then:
                                        - action: input_boolean.turn_on
                                          target:
                                            entity_id: input_boolean.night_climate_fan_by_room
                                        - action: system_log.write
                                          data:
                                            level: warning
                                            logger: night_climate
                                            message: >-
                                              Roof fan sat idle on its own thermostat with the room at
                                              {{ room }} °C (target {{ target }}): running it from the room sensor
                                    - action: script.night_climate_fan_on
                                      data:
                                        speed: "{{ fan_settings.speed }}"
                                        direction: "{{ fan_settings.direction }}"
                              default:
                                - action: script.night_climate_fan_auto
                                  data:
                                    target_c: "{{ target }}"
                                    direction: "{{ fan_settings.direction }}"
                                    speed: "{{ fan_settings.speed }}"
                      # Nothing can cool right now: do not leave the fan running for nothing
                      default:
                        - if:
                            - "{{ fan_thermo or (fan_on and fan_by_room) }}"
                          then:
                            - action: script.night_climate_fan_off

                # In band, fan run from the room sensor: off near the target, or
                # when outside stops helping (1.5 °C of hysteresis, few beeps)
                - conditions: "{{ fan_on and fan_by_room }}"
                  sequence:
                    - if:
                        - "{{ not use_fan or not fan_keep or (room is not none and room <= target + 0.5) }}"
                      then:
                        - action: script.night_climate_fan_off
                      else:
                        - action: script.night_climate_fan_on
                          data:
                            speed: "{{ fan_settings.speed }}"
                            direction: "{{ fan_settings.direction }}"
                # In band, fan on its thermostat: keep it in step; off when outside stops helping
                - conditions: "{{ fan_thermo }}"
                  sequence:
                    - if:
                        - "{{ use_fan and fan_keep }}"
                      then:
                        - action: script.night_climate_fan_auto
                          data:
                            target_c: "{{ target }}"
                            direction: "{{ fan_settings.direction }}"
                            speed: "{{ fan_settings.speed }}"
                      else:
                        - action: script.night_climate_fan_off
                # Heater running in band: keep its target in step
                - conditions: "{{ heater_on and use_heater }}"
                  sequence:
                    - action: script.night_climate_heater_to
                      data:
                        target: "{{ target }}"

# The roof fan hears one-way IR, so HA only assumes a frame landed. If the motor
# draws power while HA has it off (fan off, thermostat off) during a program or
# within an hour of one ending, the off frame was missed: on 2026-09-18 the lid
# stayed open in auto and the fan gulped 8 °C air every 12 min against the
# heater. Send it again. Outside that window the fan is left alone, it may be
# the handheld remote.
- id: night_climate_fan_watchdog
  alias: "Night Climate – roof fan runs while it should be off"
  mode: single
  max_exceeded: silent
  trigger:
    - trigger: numeric_state
      entity_id: sensor.roof_fan_power_12v
      above: 5
      for: "00:00:12"
  condition:
    - "{{ is_state('fan.ag_pro_roof_fan', 'off') and is_state('switch.ag_pro_roof_fan_thermostat', 'off') }}"
    - >
      {{ states('input_select.night_climate_mode') not in ['Off', 'unknown', 'unavailable']
         or (now() - states.input_select.night_climate_mode.last_changed).total_seconds() < 3600 }}
    - >
      {{ states.button.ag_pro_roof_fan_force_off is not none
         and not is_state('button.ag_pro_roof_fan_force_off', 'unavailable') }}
  action:
    - action: button.press
      target:
        entity_id: button.ag_pro_roof_fan_force_off
    - action: system_log.write
      data:
        level: warning
        logger: night_climate
        message: >-
          Roof fan draws {{ states('sensor.roof_fan_power_12v') }} W while HA has it off:
          sent off / lid closed again
    # One re-send per burst at most
    - delay: "00:01:00"

# Shore power has no sensor of its own; the charger's draw is the only sign and
# it reads 0 W whenever the battery is full. binary_sensor.shore_power_present
# (template/night_climate.yaml) = the charger drew power in the last 3 h; this
# keeps the time stamp. An hour of the A/C drawing power with the charger silent
# all the while means unplugged (on shore it would have topped up by then), so
# that clears it and the program stops the A/C.
- id: shore_power_seen
  alias: "Shore power – remember when the charger last drew power"
  mode: queued
  max: 5
  max_exceeded: silent
  trigger:
    - trigger: numeric_state
      entity_id: sensor.shore_power_charger_power_24v
      above: 50
      id: seen
    - trigger: time_pattern
      minutes: "/10"
      id: seen
    - trigger: state
      entity_id: binary_sensor.ag_pro_24v_ac_power_detected
      to: "on"
      for: "01:00:00"
      id: ac_hour
  action:
    - choose:
        - conditions:
            - condition: trigger
              id: seen
            - "{{ states('sensor.shore_power_charger_power_24v') | float(0) > 50 }}"
          sequence:
            - action: input_datetime.set_datetime
              target:
                entity_id: input_datetime.shore_power_last_seen
              data:
                datetime: "{{ now().strftime('%Y-%m-%d %H:%M:%S') }}"
        - conditions:
            - condition: trigger
              id: ac_hour
            - "{{ states('sensor.shore_power_charger_power_24v') | float(0) <= 50 }}"
            - >
              {{ as_timestamp(now())
                 - (state_attr('input_datetime.shore_power_last_seen', 'timestamp') | float(0)) > 3600 }}
          sequence:
            - action: input_datetime.set_datetime
              target:
                entity_id: input_datetime.shore_power_last_seen
              data:
                datetime: "1970-01-02 00:00:00"
