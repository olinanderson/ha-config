# ================================
# mqtt/sensors.yaml
# ================================
# Most WiCAN OBD data uses native ha-wican integration (HTTP webhooks) → sensor.192_168_10_90_*
#
# NOTE: Two MQTT-only sensors (wican_fuel_rate, wican_alt_duty) were removed 2026-04-14:
#   - wican_fuel_rate (22F49D): returned constant 8.66 on every poll, never changed — broken PID
#   - wican_alt_duty (220598): never published any data at all
# Both PIDs are now confirmed non-functional on this ECU. Remove from WiCAN Automate tab if desired.
