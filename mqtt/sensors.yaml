# ================================
# mqtt/sensors.yaml
# ================================
# WiCAN MQTT sensors removed — now using native ha-wican integration (HTTP webhooks).
# All WiCAN OBD data flows through sensor.192_168_10_90_* entities.
